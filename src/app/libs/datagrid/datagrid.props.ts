export enum Actions {
  group = 'group',
  sort = 'sort',
  pinLeft = 'pinLeft',
  filter = 'filter',
  reset = 'reset',
  column = 'column',
}

export enum Operators {
  contains = 'contains',
  eq = 'eq',
  equals = 'equals',
}
